const inputForm = document.querySelector('input-form'); 

const binBtn = document.querySelectorAll('bin-icon');

inputForm.addEventListener('submit', e => {
    e.preventDefault();

    const newTask = document.querySelector('input-field');
})
